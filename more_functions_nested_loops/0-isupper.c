#include "main.h"

/**
 *_isupper - Function that check for uppercase character
 *
 *Return: 1 if is uppercase 0 otherwise0
 */
int _isupper(int c)
{
	if (c >= 65 && c <= 90)
		{
		return (1);
		}
		return (0);
}
