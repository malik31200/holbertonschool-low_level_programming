#include "main.h"

/**
 *_strlen - Returns the lenght of a string
 *@s: is a pointer
 *Return: void
*/
int _strlen(char *s)
{
	int count;

	for (count = 0; s[count] != '\0'; count++)
	{
	}
	return (count);
}
