#include <stdio.h>

/**
* main - Prints all single digit numbers of base 10 starting from 0
* Return:always 0
*/

	int main(void)

{
	char hpla;

	for (hpla = 122; hpla >= 97; hpla--)

	putchar (hpla);
	putchar ('\n');
	return (0);
}
